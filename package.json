{
  "name": "gsid-benchmark",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run lint && node --test",
    "lint": "eslint . && prettier --check \"**/*.js\" \"**/*.mjs\"",
    "fix": "eslint . --fix && prettier --write \"**/*.js\" \"**/*.mjs\""
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.10.0",
    "eslint-config-metarhia": "^9.1.1",
    "prettier": "^3.3.3"
  }
}
